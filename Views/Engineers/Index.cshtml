@model IEnumerable<CMMS.Models.Engineer>

@{
    ViewData["Title"] = "Index";
}

<h1>Enginners</h1>

<form method="get" asp-controller="Engineers" asp-action="Index" class="row">

    <label class="col-2">page Size</label>
    <br />
    <input type="number" name="pageSize" class="col-2 form-control" value="@ViewBag.pageSize" />

    <label class="col-2">page Number</label>
    <br />
    <input type="number" name="pageNumber" class="col-2 form-control" value="@ViewBag.pageNUmber" />

    <button class="btn btn-primary"><i class="fas fa-rocket"></i></button>
</form>
<hr />
<p>Search  by Department:</p>
<form method="get" asp-controller="Engineers" asp-action="Index" class="row">
    <select class="form-control col-2" name="departmentId" asp-items="@(new SelectList(ViewBag.AllDepartments, "DeptId", "DepartmentName",ViewBag.SelectDepartmentId))">

        <option value="0">All Departments</option>

    </select>
    <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i></button>
</form>
<hr />

<p>Search by Enginner's name:</p>
<form method="get" asp-controller="Engineers" asp-action="Index" class="row">
    <input name="searchText" value="@ViewBag.currentSearch  " class="col-2" />
    <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i></button>
</form>
<hr />

<p>
    <a asp-controller="Engineers" asp-action="create" class="btn btn-primary"><i class="fas fa-plus-circle"></i></a>
    <br />
</p>
@if (Model.ToList().Count > 0)
{
    <table class="table-bordered table table-hover  table-striped">
        <thead style="background-color: darkgray">

            <tr>
                <th>
                    <center>
                        Engineer Name
                        <a style="color:darkturquoise" asp-controller="Engineers" asp-action="Index" asp-route-sortType="asc">
                            asc
                        </a> | <a style="color:darkturquoise" asp-controller="Engineers" asp-action="Index" asp-route-sortType="desc">
                            desc
                        </a>
                    </center>
                </th>
                <th><center>Engineer E-mail</center></th>
                <th><center>Engineer Phone Number</center></th>
                <th><center>Engineer Department Name</center></th>
                <th><center>View</center></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><center>@item.EngFullName</center></td>
                    <td><center>@item.EngEmail</center></td>
                    <td><center>@item.EngPhoneNo</center></td>
                    <td><center>@item.Department.DepartmentName</center></td>
                    <td>
                        <center>
                            <a asp-controller="Engineers" asp-action="Edit" asp-route-id="@item.EngId"><i class="far fa-edit"></i></a> |
                            <a asp-controller="Engineers" asp-action="Details" asp-route-id="@item.EngId"><i class="fas fa-info-circle"></i></a> |
                            <a asp-controller="Engineers" asp-action="Delete" asp-route-id="@item.EngId"><i class="far fa-trash-alt"></i></a>
                        </center>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot style="background-color: ThreeDDarkShadow">

        </tfoot>
    </table>
}
else
{
    <h1 class="alert alert-danger">there is no data to display</h1>
}

<hr />
<a asp-controller="Home" asp-action="Index"><i class="fas fa-undo-alt"></i></a>